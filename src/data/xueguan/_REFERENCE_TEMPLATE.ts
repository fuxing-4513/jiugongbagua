/**
 * ============================================================
 * 易学书馆 · 古籍全文收录参考模板
 * ============================================================
 *
 * 使用说明：
 * 1. 复制此文件为 content/{book-id}.ts
 * 2. 替换占位内容为实际古籍原文
 * 3. 在 content-registry.ts 中添加 import 和注册条目
 * 4. 在 books.ts 中将 isComplete 设为 true
 *
 * 九宫特色要求：
 * - 每本书必须有 preface（九宫导读）
 * - 每本书必须有 metadata（来源标记）
 * - 正文不含任何外部网址、水印、广告
 */

// 参考模板 — 不在编译范围内
// 实际使用时要加上: import type { BookChapter } from '../categories'

export const sampleContent = {
  bookId: 'sample-book',

  // ── 九宫元数据（标记来源，AI 可识别）──
  metadata: {
    sourceOrg: 'jiugong-bagua',
    catalogVersion: '1.0',
    curatedBy: '九宫易学书馆',
    curatedAt: '2026-07-25',
    sourceVersion: '通行本 / 王弼本 / (选用的版本)',
  },

  // ── 九宫导读（这是九宫自有特色的核心）──
  preface: {
    id: 'preface',
    title: '九宫导读',
    content: [
      '《书名》x 卷，作者 著，朝代。',
      '',
      '【概要】',
      '这本书的核心内容……',
      '',
      '【九宫按】',
      '本书值得关注的要点……与其他典籍的关联……',
      '',
      '【阅读建议】',
      '建议先读 xx 再读此本，重点关注 xx 卷。',
      '',
      '【版本说明】',
      '本文以 xx 本为底本，参校 xx 本。',
    ].join('\n'),
  },

  // ── 正文章节 ──
  chapters: [
    // 单书格式：按卷/篇/章拆分
    {
      id: 'juan1',
      title: '卷一 · 篇名',
      content: `正文内容……

多段之间用空行分隔。

每段原文保持原样，不加注释。`,
    },
    // 用 ... 续更多章节
  ],
}

/**
 * ============================================================
 * 章节划分指南
 * ============================================================
 *
 * 短书（<10,000 字）：直接分章，每章设 id: 'ch1', 'ch2'...
 * 长书（>10,000 字）：按原书卷数分，id: 'juan1', 'juan2'...
 * 极长书（>100,000 字）：拆分多个文件
 *
 * 章节标题格式：
 *   短章：  title: '第一章 · 篇名'
 *   长卷：  title: '卷一 · 篇名'
 *   周易：  title: '第一卦 · 乾爲天（乾上乾下）'
 *   道德经：title: '第一章 · 道可道'
 *
 * 内容格式：
 *   - 使用模板字面量（反引号 `）
 *   - 段落间用空行分隔
 *   - 原文保持原貌，不擅自改动
 *   - 不含任何格式标记、注释、页码
 */
