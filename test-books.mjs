// Import the TS module by using ts-node or dynamic import
// Actually, let me use a simpler approach: run tsc to compile and then test
import { execSync } from 'child_process';
console.log('Testing...');
